{
  "functions": {
    "app/api/*/route.ts": {
      "maxDuration": 30
    }
  },
  "regions": ["iad1"],
  "buildCommand": "pip install -r requirements.txt && python scripts/fetch_crypto_data.py && python scripts/train_model.py && python scripts/predict.py && npm run build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    }
  ]
}